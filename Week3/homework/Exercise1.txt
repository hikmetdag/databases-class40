1-What columns violate 1NF?

- food_code and food_description columns violate this rule, because every column should only contain a single value

- There is no primary keys.

- dinner_date column data are not the same type.


2- What entities do you recognize that could be extracted?

-Members,Dinner,Venue,Food.

3- Name all the tables and columns that would make a 3NF compliant solution.
-Member Table
 member_id(PK) | member_name | member_address | dinner_id(FK)

-Dinner Table
 dinner_id(PK) | dinner_date | venue_code(FK)

-Venue Table
 venue_code(PK) | venue_description

-Food Table
 food_code(PK) | food_description

Relational table Dinner-Food
id(PK) | dinner_id (FK) | food_code(FK)